# Core web framework (not used directly but dependencies need it)
fastapi>=0.100.0

# LangChain and OpenAI
langchain-core>=0.1.0
langchain-openai>=0.0.5
openai>=1.0.0

# Geocoding
geopy>=2.4.0

# Math operations for embeddings
numpy>=1.24.0

# HTTP requests for Overpass API
requests>=2.28.0

# Environment variables
python-dotenv>=1.0.0
